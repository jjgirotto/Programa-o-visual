@model IEnumerable<ESTBooks_B.Models.BookStore_B>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Location_B)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Location_B)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BookStore_BId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BookStore_BId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BookStore_BId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
